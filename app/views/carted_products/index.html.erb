<h1>Your Chairs</h1>
<% @carted_products.each do |carted_product| %>
  <div>
    <h2>Name: <%= Chair.find_by(id: carted_product.chair_id).name %></h2>
   <h2>Price: <%= Chair.find_by(id: carted_product.chair_id).price %></h2>
    <img src=<%= carted_product.chair.images[0].url %>>
    <h1>
  </div>
 <%= form_tag "/carted_products/#{carted_product.id}", method: :delete do %>
    <input type="submit" value="Remove from cart">
  <% end %>
  <% end %>

<%= form_tag "/orders", method: :post do %>
  <input type="submit" value="Buy Now!">
<% end %>